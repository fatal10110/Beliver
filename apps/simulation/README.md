# Simulation Service (Go)

Minimal scaffold for the deterministic simulation service.

## Endpoints
- `GET /health` -> `ok`
- `POST /run-match` -> stub response (not implemented)

## Run locally
```
SIM_PORT=8081 go run .
```
