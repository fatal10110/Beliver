{
  "name": "simulation",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "GOFLAGS=\"-ldflags=-linkmode=external\" go run .",
    "start": "GOFLAGS=\"-ldflags=-linkmode=external\" go run ."
  }
}
